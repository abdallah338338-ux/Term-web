# TermBoard — المسار الكامل والنهائي للمشروع (من الصفر للتشغيل الكامل)

> هذا الملف هو المرجع الوحيد المطلوب لإكمال المشروع. لو أنت (أو أي AI/code
> editor بعدك) هتكمل من أي نقطة، اقرأ القسم "0" و"1" الأول، وبعدين نفّذ
> الأقسام بالترتيب. كل قرار اتاخد في المشروع ده وكل مشكلة ظهرت وحلها
> موثّقين هنا — مفيش حاجة محتاجة "سؤال إضافي" غير مذكورة تحت.

---

## 0) ملخص تنفيذي (اقرأه أول حاجة)

**المشروع**: TermBoard — لوحة شخصية لعبدالله (طالب هندسة معمارية) تتبّع
محاضرات 4 مواد دراسية، وبتولّد امتحانات مقالية (10 أسئلة، صفحتين A4:
أسئلة ثم إجابات) من الأسئلة المستخرجة أوتوماتيك من ملفات PDF بترفعها.

**الحالة الحالية المؤكدة**:
- ✅ الواجهة الأمامية (`term-tracker.html`) **جاهزة ومُختبَرة فعليًا** —
  رُندرت في متصفح حقيقي (Playwright) من غير أي خطأ JavaScript، وفيها
  شريط إحصائيات البنك وقسم "امتحاناتي السابقة" شغّالين بالكامل.
- ✅ منطق الرفع وزرار "إنشاء امتحان جديد" موجودين ومربوطين بأربع روابط:
  `N8N_EXTRACT_URL`, `N8N_GENERATE_URL`, `N8N_BANK_INFO_URL`,
  `N8N_EXAM_HISTORY_URL` (فوق في قسم `<script>`) — الاسم فيه "N8N" من
  التسمية القديمة بس دلوقتي بيشاور على الباك إند الخفيف (قسم 2).
- ⏳ الناقص الوحيد: نشر الباك إند على Render وتحديث الروابط الأربعة.

**القرار المعماري النهائي (لا تغييره تاني)**:
**سيرفر Node.js خفيف مخصّص** (مش n8n) على **Render.com** (خطة Node
العادية، مش Docker) + قاعدة بيانات **Supabase** + **Gemini API
المجانية** + منع النوم عبر **cron-job.org**.

**تحديث مهم — ليه اتشال n8n من الاستضافة الدائمة**:
جُرّب فعليًا نشر n8n (الصورة الكاملة) على Render وKoyeb، وفشل في
الاتنين بنفس السبب: **512MB رام (سقف الطبقة المجانية في كل منصّة PaaS
مجانية تقريبًا) مش كافية لـn8n الكامل** — بيكراش بـOut of Memory
و/أو ENOSPC عند التشغيل. البديل: **سيرفر Node.js مخصّص بسيط** (ملفات
`termboard-backend/`) بيعمل بالظبط نفس الأربع وظائف بس باستهلاك رام
أقل بكتير (50-100MB)، فبيشتغل مريح جدًا على نفس خطة Render المجانية.
تفاصيل كاملة في قسم 4 وقسم 6.

**تحديث تاني — البطاقة الافتراضية المدفوعة مسبقًا (Payoneer وشبهها)
مرفوضة من Google Cloud رسميًا** (توثيق Google بيرفض الـPrepaid Cards
صراحة)، فـGCP e2-micro بقى خارج الخيارات المتاحة فعليًا إلا لو حصلنا
على كارت بنكي حقيقي غير مدفوع مسبقًا. مفيش داعي نرجع نقترحه تاني من
غير كارت بنكي حقيقي مؤكد.

**ليه القرار ده بالذات ومش حاجة تانية**: راجع قسم 6 تحت — فيه كل
البدائل اللي اتجرّبت أو اتقيّمت وليه اتقبلت أو اتّرفضت، عشان محدش
(إنسان أو AI) يرجع يقترح حاجة جُرّبت قبل كده وثبت إنها مش مناسبة.

---

## 1) العقد التقني بين الموقع والباك إند (لا يتغيّر)

### أ) استخراج الأسئلة — `POST {N8N_EXTRACT_URL}`
`multipart/form-data` بالحقول:
```
subject_id       → نص (معرّف المادة، 0-3)
subject_name     → نص (اسم المادة)
lecture_number   → نص يمثل رقم صحيح (ترتيب رفع الملف)
file             → الملف الفعلي (PDF)
```
رد النجاح: أي HTTP 200 (الموقع بيتعامل معاه كـ"تم الحفظ").

### ب) توليد الامتحان — `POST {N8N_GENERATE_URL}`
JSON:
```json
{ "subject_id": "0", "subject_name": "اسم المادة", "lecture_count": 5, "question_count": 10 }
```
رد متوقّع (JSON، **لازم يكون بالظبط بالشكل ده**):
```json
{ "questions_html": "<ol>...</ol>", "answers_html": "<ol>...</ol>" }
```
الأسئلة **مفتوحة (Open-ended)** — سؤال ونصّه، إجابته نصّها. **ممنوع MCQ
أو أي اختيارات خالص.**

### ج) إحصائيات البنك — `GET {N8N_BANK_INFO_URL}?subject_id=0`
رد متوقّع (JSON):
```json
{ "lecture_count": 5, "total_questions": 24 }
```
`lecture_count` = أعلى رقم محاضرة موجود فعليًا في بنك المادة دي.
`total_questions` = إجمالي عدد الأسئلة المخزّنة للمادة دي.

### د) الامتحانات السابقة — `GET {N8N_EXAM_HISTORY_URL}?subject_id=0`
رد متوقّع (JSON) — مصفوفة، الأحدث أولًا:
```json
[
  { "id": "...", "created_at": "2026-09-01T10:00:00Z",
    "questions_html": "<ol>...</ol>", "answers_html": "<ol>...</ol>" }
]
```
**مهم**: لازم `questions_html`/`answers_html` يتخزنوا في `exam_history`
بنفس الصيغة اللي ورشة `generate-exam` بترجّعها بالظبط (مش JSON خام
للأسئلة)، عشان الموقع يقدر يفتحهم تاني بنفس الفورمات من غير أي تحويل
إضافي.

---

## 2) خطوات الإعداد الكاملة (نفّذها بالترتيب ده بالظبط)

### 2.1 — إنشاء قاعدة بيانات Supabase (5 دقايق)
1. روح `supabase.com` وسجّل بالإيميل بس (بلا بطاقة).
2. `New Project` — اسمه `termboard`، اختار باسورد قوي واحفظه، منطقة
   قريبة (Frankfurt/London).
3. من `Project Settings → Database` انسخ: `Host`, `Port` (عادة 5432),
   `Database name` (عادة `postgres`), `User` (عادة `postgres`), `Password`.

### 2.2 — نشر الباك إند الخفيف على Render (10 دقايق)
الملفات الجاهزة موجودة في فولدر `termboard-backend/` (server.js،
package.json، README.md) المرفق مع المشروع.

1. ارفع فولدر `termboard-backend/` على GitHub repo جديد (لو معندكش
   حساب GitHub، اعمل واحد بالإيميل — مجاني).
2. روح `render.com` وسجّل بالإيميل (بلا بطاقة خالص).
3. `New → Web Service` → اربطه بالـrepo اللي رفعته. **اختار
   Environment: Node** (مش Docker هالمرة).
4. Build Command: `npm install` — Start Command: `npm start`
5. تحت `Environment` ضيف المتغيرات دي:
```
DB_HOST=<host من Supabase>
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=<password من Supabase>
GEMINI_API_KEY=<مفتاحك من aistudio.google.com — قسم 2.3>
```
6. دوس `Deploy`. لما يخلص هتاخد رابط شكله
   `https://termboard-backend-xxxx.onrender.com`. أول تشغيل هيعمل
   جداول `question_bank` و`exam_history` في Supabase تلقائيًا.
7. افتح الرابط في المتصفح — المفروض تشوف "TermBoard backend is
   running ✅". لو ظهرت، الباك إند شغّال صح.

### 2.3 — الحصول على مفتاح Gemini API
1. روح `aistudio.google.com` واخد مفتاح API مجاني (بلا بطاقة).
2. حطّه في متغيّر `GEMINI_API_KEY` على Render (قسم 2.2 خطوة 5).

### 2.4 — الأربع وظائف كلها جاهزة جوه الكود، مفيش ورشات تتبني
الباك إند الخفيف (`termboard-backend/server.js`) فيه الأربع نقاط
(`/webhook/extract-questions`, `/webhook/generate-exam`,
`/webhook/bank-info`, `/webhook/exam-history`) مكتوبة بالفعل ومطبّق
فيها بالحرف نفس المنطق المتفق عليه:

- **استخراج الأسئلة**: بيبعت ملف الـPDF كامل مباشرة لـGemini
  (multimodal) مع بروبمت يطلب فهم المعادلات والرسومات بصيغة LaTeX،
  ويخزّن كل سؤال في `question_bank` (subject_id, lecture_number,
  question, answer, type, used).
- **توليد الامتحان**: بيطبّق قاعدة "50% من آخر 3 محاضرات + 50% من
  الباقي" بالحرف (أو توزيع عشوائي لو المحاضرات 3 أو أقل)، بيفضّل
  الأسئلة اللي `used=false`، بيعلّم المُختارة `used=true`، وبيحفظ نسخة
  كاملة في `exam_history`.
- **bank-info / exam-history**: استعلامات مباشرة من نفس الجداول.

**مفيش حاجة تُبنى يدوي هنا** — لو محتاج تعدّل منطق (مثلاً تغيّر عدد
الأسئلة أو قاعدة التوزيع)، عدّل الكود في `server.js` مباشرة مش هتحتاج
تفتح أي واجهة سحب-وإفلات.

### 2.5 — حدّث الموقع
افتح `term-tracker.html`، لاقي السطور دي فوق في أول `<script>`:
```javascript
const N8N_EXTRACT_URL      = "...";
const N8N_GENERATE_URL     = "...";
const N8N_BANK_INFO_URL    = "...";
const N8N_EXAM_HISTORY_URL = "...";
```
بدّلهم بروابط Render الحقيقية (نفس الدومين، بس المسار مختلف لكل وظيفة
— بسيطة ومباشرة، مفيش IDs عشوائية زي n8n القديم):
```javascript
const N8N_EXTRACT_URL      = "https://termboard-backend-xxxx.onrender.com/webhook/extract-questions";
const N8N_GENERATE_URL     = "https://termboard-backend-xxxx.onrender.com/webhook/generate-exam";
const N8N_BANK_INFO_URL    = "https://termboard-backend-xxxx.onrender.com/webhook/bank-info";
const N8N_EXAM_HISTORY_URL = "https://termboard-backend-xxxx.onrender.com/webhook/exam-history";
```
**من غير أي تعديل تاني في التصميم أو المنطق — الموقع فعليًا جاهز
بالكامل ومربوط بالأربع نقاط دي، مستني بس الروابط الحقيقية.**

### 2.6 — امنع نوم الخدمة
1. سجّل مجانًا على `cron-job.org`.
2. اعمل Cron Job يطلب `https://termboard-backend-xxxx.onrender.com` كل
   10-13 دقيقة (أقل من حد النوم 15 دقيقة، وأقل من حد الساعات الشهري 750).

---

## 3) اختبار نهائي (نفّذه بالترتيب، ولا تعتبر المشروع خلص إلا لو الكل ✅)

- [ ] رفعت PDF حقيقي من الموقع → ظهرت حالته "✅ في البنك".
- [ ] فتحت جدول `question_bank` في Supabase (Table Editor) → لقيت
      أسئلة حقيقية منظّمة (مش نص خام).
- [ ] دوست "إنشاء امتحان جديد" → فتح تاب/مودال فيه صفحتين A4 فعليًا.
- [ ] زرار "إظهار الإجابات" شغّال ويوري الإجابات الصح.
- [ ] زرار الطباعة بيفتح معاينة طباعة بمقاس A4 صح.
- [ ] شريط "X محاضرة في البنك — Y سؤال متاح" ظاهر تحت اسم المادة
      بأرقام حقيقية (مش "جارٍ التحميل" واقفة).
- [ ] دوست "📚 امتحاناتي السابقة" → ظهرت قائمة فيها الامتحان اللي
      عملته، ودوست عليه → فتح بنفس محتواه الأصلي.
- [ ] قفّلت اللابتوب/سبت البيت، وجرّبت الموقع من الموبايل عبر بيانات
      الموبايل (مش الواي فاي) — واشتغل بنفس الطريقة.
- [ ] استنيت أكتر من 15 دقيقة من غير استخدام، وجرّبت تاني — اشتغل من
      غير تأخير محسوس (يعني البينج شغّال صح).
- [ ] لسه مفيش أي فلوس اتخصمت في أي خطوة.

---

## 4) كل المشاكل اللي ظهرت فعليًا أثناء البناء وحلولها (مرجع لأي مشكلة تتكرر)

| المشكلة | السبب | الحل |
|---|---|---|
| نص JavaScript خام ظاهر على الصفحة بدل ما يشتغل | نسخة قديمة من الموقع كانت بتعتمد على سيرفر Python محلي (`BRIDGE_URL=127.0.0.1:8787`, `start_server.bat`) بدل n8n | اتشال بالكامل، الموقع دلوقتي بيستخدم `N8N_EXTRACT_URL`/`N8N_GENERATE_URL` بس |
| الموقع فاتح ملف قديم رغم إنك بعتّله الجديد | المتصفح فاتح تاب قديم من ملف بنفس الاسم في الداونلودز | اقفل التاب القديم قبل ما تفتح الملف الجديد |
| `https://termboard.Zero Trust/...` مش شغّال | placeholder اتنسي يتستبدل باسم حقيقي | كان لازم يتستبدل برابط Cloudflare حقيقي — دلوقتي مش محتاجينه أصلًا بعد الانتقال لـRender |
| `[team].cloudflareaccess.com` مارضيش يوصل للـwebhook | ده دومين تسجيل دخول Zero Trust Access بس، مش رابط توجيه عام | متستخدموش لتوجيه webhooks خالص |
| DuckDNS مع Cloudflare Named Tunnel مش شغّال | لازم الدومين يكون "zone" فعلي جوه حسابك في Cloudflare، وDuckDNS مش دومين انت مالكه | اتحل بالانتقال الكامل لـRender (مفيش تعامل مع دومينات أو أنفاق خالص) |
| Quick Tunnel بيموت وبيدّي رابط جديد كل مرة | طبيعة Quick Tunnel — مؤقت بتصميمه | اتحل نهائيًا بالانتقال لرابط Render الثابت |
| Fly.io مقترَح كخيار مجاني | الطبقة المجانية اتشالت من 2024، لازم بطاقة دلوقتي | مستبعد من الاختيار |
| Oracle Cloud مقترَح كخيار مجاني | قلّلوا حدود الطبقة المجانية للنص في أغسطس 2026 من غير إعلان، وفيه خطر توقيف حسابات | مستبعد من الاختيار |
| Google Cloud طلب تفعيل الفوترة بالفيزا | إجراء تحقّق هوية عادي، مش معناه هتتخصم فلوس فعليًا | اتقرر الانتقال لـRender نهائيًا لراحة البال (مفيش بطاقة خالص) |
| بيانات بنك الأسئلة اختفت بعد فترة | Render المجاني بيمسح القرص المحلي (SQLite) كل ما الخدمة تنام وتصحى | اتحل بربط n8n بقاعدة بيانات Supabase خارجية دائمة بدل SQLite المحلي |
| قاعدة بيانات Render نفسها (مش Supabase) | الطبقة المجانية بتاعتها بتنتهي/تتمسح بعد 30 يوم بالظبط | متتستخدمش خالص — استخدم Supabase فقط |
| الخدمة بتاخد ~50 ثانية أول طلب بعد فترة خمول | Render بينوّم الخدمة المجانية بعد 15 دقيقة خمول | بينج خارجي من cron-job.org كل 10-13 دقيقة يمنع النوم تمامًا |
| Supabase ممكن "يوقف" المشروع | بيوقف تلقائي بعد 7 أيام من غير أي نشاط API | بيتحل تلقائيًا لو الموقع بيتستخدم بانتظام (البينج نفسه كافي) |
| ورشة كانت رافضة تتفعّل في n8n المحلي القديم | مشكلة غير مُشخَّصة في n8n نفسه؛ محاولات سابقة حلّتها بتعديل قاعدة بيانات SQLite الداخلية مباشرة بالـSQL (خطر ومش مضمون) | **ممنوع تكرار الأسلوب ده خالص**. على السيرفر الجديد (Render+Supabase) هتبدأ بقاعدة بيانات نضيفة؛ لو التفعيل رفض تاني، امسح الورشة واستوردها من جديد بدل أي تلاعب في الجداول الداخلية |
| مفتاح Gemini API كان مكتوب مكشوف جوه رابط HTTP Request node | سهولة وقت الاختبار، لكن غير آمن (أي حد يشوف الورشة أو نسخة منها بيشوف المفتاح) | انقله لـCredential من نوع Header Auth جوه n8n، وأعد توليد (Regenerate) أي مفتاح اتكشف قبل كده فورًا من aistudio.google.com |
| n8n بيكراش على Render (Out of Memory + ENOSPC) | صورة n8n الكاملة محتاجة رام أكتر من 512MB (سقف الطبقة المجانية شبه الموحّد في كل منصّات PaaS المجانية)، ومساحة قرص أكبر من المتاح | **اتحل جذريًا**: استُبدل n8n بالكامل بسيرفر Node.js خفيف مخصّص (`termboard-backend/`) بيعمل نفس الوظائف الأربعة باستهلاك رام أقل بكتير (50-100MB). **ممنوع الرجوع لاستضافة n8n الكامل على أي PaaS مجاني — جُرّب فعليًا وفشل مرتين (Render وKoyeb)** |
| بطاقة Payoneer/فيزا افتراضية مدفوعة مسبقًا مرفوضة من Google Cloud | توثيق Google الرسمي بيرفض الـPrepaid Cards صراحة لأي Cloud Billing account | GCP e2-micro **مستبعد فعليًا** إلا لو توفّر كارت بنكي حقيقي (مش مدفوع مسبقًا) بيدعم الدفع المتكرر التلقائي |

---

## 5) خارج نطاق التنفيذ الحالي (مؤجّل، اذكره بس لو المستخدم طلبه صراحة)

- نقل بيانات المواد نفسها (الاسم/الأيقونة/اللون) من تخزين المتصفح
  المحلي لجدول جوه Supabase عشان تتزامن هي كمان بين الأجهزة (دلوقتي
  كل جهاز عنده أسماء/ألوان مختلفة محليًا، وده مش مشكلة كبيرة لأن
  الأسئلة والامتحانات نفسها متزامنة بالفعل عبر Supabase).

**ملحوظة**: قسم "امتحاناتي السابقة" وإحصائيات بنك الأسئلة **اتنفّذوا
بالكامل** في الفرونت إند (زرار + قائمة + إعادة فتح) — الناقص بس هو
بناء ورشتي `bank-info` و`exam-history` في n8n (قسم 2.6 و2.7 فوق) عشان
يردّوا على الطلبات اللي الموقع بيبعتها أصلًا.

---

## 6) أرشيف القرارات (ليه اتقبل كل حاجة وليه اترفضت التانية)

1. **جسر Python محلي + Antigravity SDK** — تقني وصحيح لكن معقّد، محتاج
   بناء سيرفر من الصفر، ومش خدمة أوتوماتيك حقيقية. **اترفض.**
2. **n8n كأداة أتمتة/بناء الأربع وظائف كورشات** — منطقي كفكرة (Webhook
   جاهز، واجهة بصرية)، لكن **اترفض لاحقًا لأسباب موارد** — راجع بند 13.
3. **Antigravity/Gemini يبني الموقع من الصفر بتصميم مختلف (وحدات،
   MCQ، ثيم غامق)** — اترفض صراحة، رجّعنا للتصميم الأصلي المتفق عليه.
4. **MCP Server Trigger بدل Webhook (وقت ما كنا لسه بنستخدم n8n)** —
   سوء فهم تقني، اترفض واستُبدل بـWebhook العادي.
5. **Cloudflare Named Tunnel بدومين حقيقي** — يحتاج شراء دومين، اترفض.
6. **Cloudflare Quick Tunnel** — شغّال لكن مؤقت (الرابط بيتغيّر)، استُخدم
   كحل مرحلي بس مش نهائي.
7. **DuckDNS/No-IP مع Cloudflare** — تقنيًا مش هيشتغل (مشكلة ملكية
   الـzone)، اترفض.
8. **Fly.io** — الطبقة المجانية ماتت من 2024، اترفض.
9. **Oracle Cloud Always Free** — أقوى مواصفات لكن غير مستقر حاليًا
   (تقليص الحدود + مخاطر توقيف حسابات)، اترفض.
10. **Google Cloud e2-micro** — قوي وموثوق تقنيًا، بس محتاج كارت بنكي
    حقيقي غير مدفوع مسبقًا، والكارت المتاح للمستخدم (Payoneer/فيزا
    افتراضية مدفوعة مسبقًا) **مرفوض رسميًا من Google Cloud**. **اترفض
    فعليًا (مش بس تفضيل شخصي) إلى أن يتوفّر كارت بنكي حقيقي.**
11. **Render.com + قاعدة بيانات Render الداخلية** — الخدمة تمام لكن
    قاعدة بياناتها المجانية بتتمسح بعد 30 يوم. **اترفض جزء قاعدة
    البيانات فقط.**
12. **Koyeb (بديل لـRender)** — نفس سقف الرام بالظبط (512MB)، فمش هيحل
    مشكلة تشغيل n8n الكامل. **اترفض كبديل استضافة لـn8n.**
13. **استضافة n8n الكامل (Docker image) على أي PaaS مجاني** — **اترفض
    نهائيًا بعد تجربة فعلية فاشلة على Render وKoyeb** (كراش Out of
    Memory + ENOSPC، لأن n8n محتاج رام أكتر من 512MB سقف الطبقة
    المجانية في كل منصّة). **البديل المعتمد: سيرفر Node.js خفيف
    مخصّص (`termboard-backend/`) بنفس المنطق بالظبط، باستهلاك رام
    50-100MB بس.**
14. **Render.com + Supabase (خارجية) + Gemini API + سيرفر Node.js خفيف
    مخصّص (بدل n8n) + cron-job.org pinger** — **القرار النهائي
    المعتمد.** بلا بطاقة، بلا نوم فعلي (مع البينج)، بلا فقدان بيانات،
    بلا تكلفة، وبلا مشاكل رام.

---

## 7) تعليمات لأي AI/code editor يكمل من هنا

```
انت بتكمل مشروع TermBoard. اقرأ الملف ده كامل قبل أي خطوة. القرار
المعماري النهائي (قسم 2 و6) نهائي ومش قابل للتغيير أو "التحسين" من
غير إذن صريح من المستخدم — خصوصًا متقترحش Fly.io أو Oracle Cloud أو
Google Cloud (من غير كارت بنكي حقيقي مؤكد) أو استضافة n8n الكامل على
أي PaaS مجاني (Render/Koyeb) أو تصميم فرونت إند مختلف — دول كلهم
اترفضوا لأسباب موثّقة فوق بعد تجربة فعلية فاشلة، مش تخمين.

**الباك إند دلوقتي سيرفر Node.js خفيف مخصّص** (فولدر
`termboard-backend/`)، **مش n8n**. الكود فيه الأربع وظائف كاملة
ومكتوبة بالفعل (استخراج الأسئلة، توليد الامتحان، إحصائيات البنك،
الامتحانات السابقة) — مفيش ورشات تُبنى أو تُستورد. أي تعديل مطلوب في
المنطق يتم مباشرة في `server.js`.

نفّذ قسم 2 بالترتيب (2.1 لحد 2.6):
1. أنشئ قاعدة بيانات Supabase (2.1).
2. ارفع `termboard-backend/` على GitHub وانشره على Render كـWeb
   Service من نوع Node (مش Docker) (2.2).
3. جيب مفتاح Gemini API وحطّه في متغيرات البيئة (2.3).
4. تأكد إن السيرفر شغّال (افتح رابط Render، المفروض تشوف "TermBoard
   backend is running ✅").
5. حدّث الروابط الأربعة في term-tracker.html (2.5).
6. اعمل بينج خارجي يمنع النوم (2.6).

بعد كل خطوة، تأكد إنها شغّالة فعليًا قبل ما تنتقل للي بعدها. لو أي
خطوة فشلت، ارجع لقسم 4 (جدول المشاكل) شوف لو نفس المشكلة موثّقة قبل
كده وحلها بنفس الطريقة. **لو n8n كراش أو ظهرت مشكلة رام — متحاولش
تصلحها أو ترجع لاستضافة n8n، الحل الجاهز هو الباك إند الخفيف ده.**

لما تخلص، نفّذ كل بنود قسم 3 (الاختبار النهائي) واحد واحد وأكّد كل
بند شغّال فعليًا قبل ما تقول للمستخدم "المشروع خلص".

الفرونت إند (term-tracker.html) **جاهز بالكامل ومفيهوش أي ناقص** —
لا يُعاد تصميمه أو تعديل منطقه إطلاقًا. التعديل الوحيد المسموح فيه هو
تحديث الروابط الأربعة (N8N_EXTRACT_URL, N8N_GENERATE_URL,
N8N_BANK_INFO_URL, N8N_EXAM_HISTORY_URL) في قسم 2.5.
```
